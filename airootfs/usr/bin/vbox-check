#!/bin/bash
#
result=$(systemd-detect-virt)
if [ $result = "none" ];
  then
  rm /etc/xdg/autostart/vboxclient.desktop
  pacman -Rsndd virtualbox-guest-utils virtualbox-guest-dkms --noconfirm
  systemctl disable vbox-check.service
fi
